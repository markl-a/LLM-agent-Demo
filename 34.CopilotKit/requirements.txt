# CopilotKit Python 依賴

# 核心框架
copilotkit>=0.1.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# LLM 集成
openai>=1.12.0
anthropic>=0.18.0
langchain>=0.1.0
langchain-openai>=0.0.5
langgraph>=0.0.20

# 數據處理
python-dotenv>=1.0.0
python-multipart>=0.0.6
aiofiles>=23.2.1

# WebSocket 支持
websockets>=12.0
python-socketio>=5.11.0

# 工具和實用程序
httpx>=0.26.0
requests>=2.31.0
pydantic-settings>=2.1.0

# 數據庫（可選）
sqlalchemy>=2.0.25
alembic>=1.13.1
redis>=5.0.1

# 監控和日誌
loguru>=0.7.2
prometheus-client>=0.19.0
sentry-sdk>=1.40.0

# 測試
pytest>=7.4.4
pytest-asyncio>=0.23.3
pytest-cov>=4.1.0
httpx-test>=0.1.0

# 開發工具
black>=24.1.0
flake8>=7.0.0
mypy>=1.8.0
isort>=5.13.2

# 前端構建（可選，用於全棧開發）
# 註：前端依賴應在 package.json 中管理
# @copilotkit/react-core
# @copilotkit/react-ui
# @copilotkit/react-textarea
# @copilotkit/runtime

# 注意事項：
# 1. 前端 npm 包需要單獨安裝：
#    npm install @copilotkit/react-core @copilotkit/react-ui
#
# 2. 環境變量配置（.env）：
#    OPENAI_API_KEY=your_openai_api_key
#    ANTHROPIC_API_KEY=your_anthropic_api_key
#    COPILOT_RUNTIME_URL=http://localhost:8000/copilotkit
#
# 3. Python 版本要求：Python 3.9+
#
# 4. 安裝命令：
#    pip install -r requirements.txt
